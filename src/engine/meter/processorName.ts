// The meter worklet's registered name — one constant shared by the worklet
// module (src/worklets/meter-processor.ts) and the main-thread bus, same
// pattern as src/devices/core/polySynth/processorName.ts.

export const METER_PROCESSOR_NAME = "fableton-meter";
